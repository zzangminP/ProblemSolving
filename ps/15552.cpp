#include <bits/stdc++.h>
using namespace std;

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int c;
	int a, b;
	cin >> c;
	while (c--)
	{
		cin >> a;
		cin >> b;
		cout << a + b << "\n";
	}
}